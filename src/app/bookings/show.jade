.settings-wrapper

  .top-interlude
    p.s-inter-content.font-size-1 Your account has been created

  section
    .section-row
      p.section-title.medium-gray Driver Documents
    .section-row(ng-repeat='item in driverDocuments')
      .row-content(ng-class='{"has-bar": !$last}')
        label.left-flex-title {{item.label}}
        .img-wrapper
          img.check-mark(ng-if='item.isUploaded', src='/assets/images/Mark_Check.png')
          img.check-mark(ng-if='!item.isUploaded', src='/assets/images/Mark_X.png')

  .section-interlude
    .b-inter-content Driver Documents are required to complete your booking.
      span.s-inter-content.medium-gray you can upload/change your documents in Account Settings page.

  .button-section
    a.button Go to Account
