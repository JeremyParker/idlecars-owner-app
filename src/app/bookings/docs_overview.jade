.settings-wrapper

  .top-interlude
    .b-inter-content Upload all your documents to complete your rental.

  section
    .section-row
      p.section-title.medium-gray Your Driver Documents
    .section-row(ng-repeat='item in driverDocuments')
      a.row-content(ng-class='{"has-bar": !$last}', ui-sref="{{item.sref}}")
        .left-flex-title {{item.title}}
        .img-wrapper
          img.check-mark(ng-if='item.isUploaded', src='/assets/images/Mark_Check.png')
          .color-red.next-pointer(ng-if='!item.isUploaded') Upload

  .section-interlude
    .b-inter-content What happens next?
    span.s-inter-content.dark-gray
      | When your documentation is complete, you will be added to your car's insurance.
      | We will contact you to pick up your car. This usually takes 24-48 hours.

  .button-section
    a.button(ui-sref='driverAccount.faq') Questions
