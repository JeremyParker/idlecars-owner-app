section.map
  ui-gmap-google-map(ng-if='mapIsVisable', center='map.center', zoom='map.zoom', options='map.options', refresh=true)
    ui-gmap-circle(center='circle.center', radius='circle.radius', stroke='circle.stroke', fill='circle.fill')

