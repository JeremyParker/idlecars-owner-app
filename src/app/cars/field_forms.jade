.field-form
  .field-container
    .nav-bar-field
      button.back-sign(ng-click='back()') <
      .title
        h2 Information
      button.next(ng-click='goNext()') next>

    .text-wrapper
      p {{label}}
      input(placeholder='{{placeholder}}' type='{{type}}' name='{{name}}' id='{{name}}' ng-model='value' novalidate required)
      hr
      input(placeholder='Last name' type='text' name='lastName' id='lastName' ng-model='lastname' novalidate ng-if='showLastName')
      hr(ng-if='showLastName')
