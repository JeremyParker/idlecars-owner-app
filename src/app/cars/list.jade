.listing-wrapper(save-scroll)
  h1(hidden) Rent cars for rideshare

  a.car(ng-repeat="car in cars | attrInRange:'cost':0:1000", ui-sref=".detail({carId: car.id, car: car})")
    car-image
    .info
      h3 {{car.name}}
      p {{car.listing_features}}
